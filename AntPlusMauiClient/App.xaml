<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AntPlusMauiClient"
             xmlns:exp="clr-namespace:Syncfusion.Maui.Toolkit.Expander;assembly=Syncfusion.Maui.Toolkit"
             xmlns:tabview="clr-namespace:Syncfusion.Maui.Toolkit.TabView;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:conv="clr-namespace:AntPlusMauiClient.Converters"
             x:Class="AntPlusMauiClient.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <conv:ByteArrayToStringConverter x:Key="ByteArrayToString"/>
            <conv:StreamToImageSourceConverter x:Key="StreamToImage"/>
            <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />

            <Style TargetType="Label" x:Key="OfflineStyle">
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="Opacity" Value="0.4"/>
                <Setter Property="FontSize" Value="48"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="Text" Value="OFFLINE"/>
            </Style>

            <Style TargetType="Border">
                <Setter Property="Margin" Value="8"/>
                <Setter Property="Padding" Value="8"/>
                <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource BlackBrush}, Dark={StaticResource WhiteBrush}}"/>
                <Setter Property="StrokeShape" Value="RoundRectangle 8"/>
            </Style>

            <Style TargetType="Label" x:Key="HeaderLabelStyle">
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="Title" />
            </Style>

            <Style TargetType="tabview:SfTabItem">
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="TextColor" Value="{AppThemeBinding Dark={StaticResource Secondary}, Light={StaticResource SecondaryDarkText}}"/>
            </Style>

            <Style TargetType="exp:SfExpander">
                <Setter Property="HeaderIconColor" Value="{AppThemeBinding Dark={StaticResource PrimaryDark}, Light={StaticResource Primary}}"/>
            </Style>

            <LinearGradientBrush x:Key="BackgroundBrush" StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="{AppThemeBinding Light=#4A6FA5, Dark=#4A6FA5}" Offset="0.0" />
                <GradientStop Color="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}" Offset="0.5" />
                <GradientStop Color="{AppThemeBinding Light=#4A6FA5, Dark=#4A6FA5}" Offset="1.0" />
            </LinearGradientBrush>

            <Style TargetType="Button" x:Key="DefaultButtonStyle"/>
            <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Padding" Value="16"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
